# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment / secrets — NEVER commit the Hevy API key or dashboard password
.env
.env.*
!.env.example

# Local sync data only; keep src/data source files tracked.
/data/
public/data/dataset.json
public/data/dataset.enc.json

# Temporary files from atomic snapshot writes
*.tmp

# Misc
.DS_Store
*.log
.vite/
